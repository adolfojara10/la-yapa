// La Yapa — design system entrypoint.
export * from './tokens';

// Back-compat aliases for code written against the v0 token shape.
export { palette as colors } from './tokens';
export { typeScale as typography } from './tokens';
